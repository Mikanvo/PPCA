{#{{ grid(grid) }}#}
<div class="table-toolbar">
    <div class="row">
        <div class="col-md-6">
            {#<div class="btn-group">
                <button id="sample_editable_1_new" class="btn sbold green"> Add New
                    <i class="fa fa-plus"></i>
                </button>
            </div>#}
        </div>
        <div class="col-md-6">
            <div class="btn-group pull-right">
                <button class="btn green  btn-outline dropdown-toggle" data-toggle="dropdown">Tools
                    <i class="fa fa-angle-down"></i>
                </button>
                <ul class="dropdown-menu pull-right">
                    <li>
                        <a href="javascript:;">
                            <i class="fa fa-print"></i> Imprimer </a>
                    </li>
                    <li>
                        <a href="javascript:;">
                            <i class="fa fa-file-pdf-o"></i> Expoter en PDF </a>
                    </li>
                    <li>
                        <a href="javascript:;">
                            <i class="fa fa-file-excel-o"></i> Exporter en Excel </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
<table class="table table-striped table-bordered table-hover table-checkable order-column" id="sample_1">
    <thead>
    <tr>
        <th> Date réception </th>
        <th> Expediteur </th>
        <th> Objet </th>
        {#<th> Contenu </th>#}
        <th> Actions </th>
    </tr>
    </thead>
    <tbody>
    {% for mail in mails %}
        <tr class="odd gradeX">
            <td> {{ mail.date|date('d/m/Y') }} </td>
            <td> {{ mail.expediteur }} </td>
            <td> {{ mail.objet }} </td>
            {#<td> {{ mail.corps }} </td>#}
            <td>
                <div class="btn-group">
                    {{ render(controller('SiseBundle:Dano:new', {'mail': mail.id, 'origine': 'mail'})) }}
                    {{ render(controller('SiseBundle:Mail:follow', { 'id': mail.id })) }}
                </div>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
