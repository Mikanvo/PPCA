{#{{ grid(grid) }}#}
<div class="table-toolbar">
    <div class="row">
        <div class="col-md-6">
            {#<div class="btn-group">
                <button id="sample_editable_1_new" class="btn sbold green"> Add New
                    <i class="fa fa-plus"></i>
                </button>
            </div>#}
        </div>
        <div class="col-md-6">
            {#<div class="btn-group pull-right">
                <button class="btn green  btn-outline dropdown-toggle" data-toggle="dropdown">Tools
                    <i class="fa fa-angle-down"></i>
                </button>
                <ul class="dropdown-menu pull-right">
                    <li>
                        <a href="javascript:;">
                            <i class="fa fa-print"></i> Print </a>
                    </li>
                    <li>
                        <a href="javascript:;">
                            <i class="fa fa-file-pdf-o"></i> Save as PDF </a>
                    </li>
                    <li>
                        <a href="javascript:;">
                            <i class="fa fa-file-excel-o"></i> Export to Excel </a>
                    </li>
                </ul>
            </div>#}
        </div>
    </div>
</div>
<table class="table table-striped table-bordered table-hover table-checkable order-column" id="sample_1">
    <thead>
    <tr>
        <th> N° </th>
        <th> Objet </th>
        <th> Requête </th>
        <th> Activite </th>
        <th> Actions </th>
    </tr>
    </thead>
    <tbody>
    {% for dano in danos %}
    <tr class="odd gradeX">
        <td> {{ dano.numero }} </td>
        <td> {{ dano.objet }} </td>
        <td> {{ dano.requete.libelle }} </td>
        <td> {{ dano.activite.libelle }} </td>
        <td>
            {% if etat == 1 %}
            <div class="btn-group">
                {{ render(controller('SiseBundle:Dano:show', { 'id': dano.id })) }}
                {{ render(controller('SiseBundle:Dano:edit', { 'id': dano.id })) }}
            </div>
            {% endif %}
            {% if etat == 2 %}
                <div class="btn-group">
                    {{ render(controller('SiseBundle:Dano:edit', { 'id': dano.id })) }}
                    {{ render(controller('SiseBundle:Dano:envoyerMailDano', { 'id': dano.id })) }}
                </div>
            {% endif %}
            {% if etat == 3 %}
                <div class="btn-group">
                    {{ render(controller('SiseBundle:Dano:show', { 'id': dano.id })) }}
                </div>
            {% endif %}
            {% if etat == 4 %}
                <div class="btn-group">
                    {{ render(controller('SiseBundle:Dano:show', { 'id': dano.id })) }}
                </div>
            {% endif %}
            {% if etat == 5 %}
                <div class="btn-group">
                    {{ render(controller('SiseBundle:Dano:show', { 'id': dano.id })) }}
                </div>
            {% endif %}

        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>